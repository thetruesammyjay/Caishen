---
title: Demo
description: Run the Caishen demo agent workflow.
---

The demo agent executes wallet + protocol quote operations and writes lifecycle events.

## Run

```bash
pnpm --filter @caishen/demo-agent start
```

## Observe

```bash
caishen monitor --tail 20
```

## End-to-end protocol CLI script

```bash
caishen protocol --chain ethereum --type swap --label velora --method quote --params '{"fromToken":"USDT","toToken":"ETH","amount":"1000000"}' --dry-run
caishen protocol --chain ethereum --type swap --label velora --method quote --params '{"fromToken":"USDT","toToken":"ETH","amount":"1000000"}' --confirm "CONFIRM PROTOCOL swap.velora.quote ON ethereum SIG <FROM_PREFLIGHT>"
```

## Outputs

- Session lifecycle events (`agent.start`, `agent.round`, `agent.end`)
- Wallet and protocol operation traces
- Chain-scoped runtime metadata

See [demo/README.md](../demo/README.md) and [walkthrough.md](../walkthrough.md).
