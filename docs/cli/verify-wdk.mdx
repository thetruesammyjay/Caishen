---
title: caishen verify-wdk
description: Validate WDK runtime capability on configured chains.
---

## Examples

```bash
caishen verify-wdk --token USDT --amount 1
caishen verify-wdk --token USDT --amount 1 --json
```

Use `--json` for CI pipelines.
